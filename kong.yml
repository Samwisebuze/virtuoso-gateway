_format_version: "1.1"

services:
- name: my-service
  url: http://127.0.0.1:8080
  routes:
  - name: my-route
    paths:
    - /
- name: dummy-service
  url: http://host.docker.internal:5000
  routes:
  - name: dummy-routes
    paths:
    - /
